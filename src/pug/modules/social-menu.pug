mixin socialMenu(menuList,menu)
    ul(class=menuList)
        each subMenu in menu
            li
                a
                    span.social-icon
                        i(href="#" class=subMenu)